using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerIcons : MonoBehaviour {
    public IngredientIconMap icm;
	// Use this for initialization
	void Awake () {
        icm.woke = false;
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
